import { HideElement } from './hide-element';

describe('HideElement', () => {
  it('should create an instance', () => {
    const directive = new HideElement();
    expect(directive).toBeTruthy();
  });
});
